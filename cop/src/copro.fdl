#include "mwi.fdl"
#include "coreg.fdl"

dp copro(
    in cmd         : ns(32);
    in datain      : ns(32);
    in datainrdy   : ns(32);

    out dataout    : ns(32);
    out dataoutrdy : ns(32)) {

    use mwi(old, curr,
            result);

    use regfile(10, 1,
                old, curr);
}
